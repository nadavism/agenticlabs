services:
  - type: web
    name: order-agentic-labs
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
